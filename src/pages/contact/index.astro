---
import BaseLayout from "../../layouts/BaseLayout.astro";
---

<BaseLayout
  title="Contact"
  description="Get in touch with Matt Rabah."
>
  <section class="hero container">
    <p class="kicker">Contact</p>

    <h1 class="display">Let's build a better lifecycle.</h1>

    <p class="lead">
      If you need a CX Manager to untangle a complex customer journey, align your teams, or design workflows that actually drive retention—let's talk.
    </p>

    <div style="margin-top: var(--s-lg);">
      <a
        href="mailto:rabah.matthew@gmail.com"
        style="
          display: inline-block;
          background: var(--c-accent);
          color: white;
          padding: 0.8rem 1.6rem;
          text-decoration: none;
          font-weight: 500;
          border-radius: var(--r-sm);
          font-family: var(--ff-sans);
          transition: background-color var(--ms-base);
        "
        onmouseover="this.style.backgroundColor='var(--c-accent-ink)'"
        onmouseout="this.style.backgroundColor='var(--c-accent)'"
      >
        Email me
      </a>
    </div>
  </section>
</BaseLayout>